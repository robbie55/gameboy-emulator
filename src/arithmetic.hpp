#pragma once

#include <cstdint>

namespace f_flag_bit_positions {
  inline constexpr uint8_t kZ{7};  // zero
  inline constexpr uint8_t kN{6};  // subtract
  inline constexpr uint8_t kH{5};  // half-carry
  inline constexpr uint8_t kC{4};  // carry
}  // namespace f_flag_bit_positions
